# HTTP client — used by OllamaPlanner (also a transitive dep of gradio)
requests>=2.31.0

# Physics + control
mujoco>=3.0.0
numpy>=1.24.0

# Video capture
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Dashboard
gradio>=6.0.0

# ---------------------------------------------------------------------------
# Optional: AWS Bedrock backend (set LLM_BACKEND=bedrock)
# pip install boto3
# ---------------------------------------------------------------------------
